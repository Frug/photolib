# PhotoLib

Demo App

To start locally, use `start-local.sh`.

If you don't have go 1.12, the binary is included at `go/photolib` and can be started manually without building. 

Use `dump-photos.sh` to dump a set of dummy photos to start with.
